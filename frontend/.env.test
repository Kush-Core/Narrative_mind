# Test-suite configuration (committed, unlike .env).
# Pinning the API origin keeps the suite hermetic: MSW handlers and the HTTP
# client agree on where the backend is, regardless of local .env values.
VITE_API_BASE_URL=http://localhost:8000

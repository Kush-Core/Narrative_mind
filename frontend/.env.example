# Narrative Mind frontend — client configuration.
# Only VITE_-prefixed, non-secret values belong here: everything in this file
# is embedded into the browser bundle at build time.

# Base URL of the backend API (defaults to http://localhost:8000 if unset).
VITE_API_BASE_URL=http://localhost:8000
